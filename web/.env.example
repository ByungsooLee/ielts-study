# ローカル開発用テンプレート。実際の値は .env.local に書く（Git 除外済み）。
# 初回: cp .env.local.example .env.local
# またはリポジトリルートで: npm run setup
#
# VITE_DEFAULT_WORKER_URL=https://ielts-study-worker.byung4050.workers.dev
# VITE_DEFAULT_SYNC_TOKEN=worker/.dev.vars と同じ SYNC_TOKEN
